{
  "name": "cycleplan",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "setup": "bun run setup.ts",
    "build": "bun build src/map.ts --outfile public/map.js --target browser --format iife",
    "dev": "bun --watch run build",
    "server": "bun run build && bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.20",
    "leaflet": "^1.9.4"
  }
}
