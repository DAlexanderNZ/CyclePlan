{
  "name": "cycleplan",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "setup": "bun run setup.ts",
    "build": "bun build src/map.ts --outdir public --target browser",
    "dev": "bun --watch run build",
    "server": "bun run build && bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.20",
    "leaflet": "^1.9.4"
  }
}
